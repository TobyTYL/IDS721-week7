[package]
description = "Convert TOML to JSON"
license = "MIT"
version = "0.0.1"

[package.metadata.release]
dev-version = false
publish = false 

[dependencies]
anyhow = "1.0"
clap = "4.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.7"